include(ExternalProject)

ExternalProject_Add(libst
    SOURCE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/st-1.9
    BUILD_COMMAND make OS=LINUX BUILD=OPT STATIC_ONLY=yes EXTRA_CFLAGS=-fPIC EXAMPLES= DESC= HEADER= 
    BUILD_IN_SOURCE 1
    DOWNLOAD_COMMAND ""
    CONFIGURE_COMMAND "" 
    INSTALL_COMMAND "" 
)

